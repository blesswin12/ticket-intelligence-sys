_schema-version: 3.3.0
ID: ticket-intelligence-sys
description: AI-Powered Customer Support System on SAP BTP
version: 1.0.0
modules:
- name: ticket-intelligence-sys-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: ticket-intelligence-sys-auth
  - name: ticket-intelligence-sys-db
  - name: ticket-intelligence-sys-destination
  - name: ticket-intelligence-sys-logging
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
    env:
      GROQ_API_KEY: your_groq_api_key_here
  build-parameters:
    builder: npm-ci
- name: ticket-intelligence-sys-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: ticket-intelligence-sys-db
  parameters:
    buildpack: nodejs_buildpack
- name: ticket-intelligence-sys
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: ticket-intelligence-sys-auth
  - name: ticket-intelligence-sys-destination
  - name: ticket-intelligence-sys-html5-runtime
  - name: ticket-intelligence-sys-logging
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: ticket-intelligence-sys-app-deployer
  type: com.sap.application.content
  path: gen
  requires:
  - name: ticket-intelligence-sys-auth
  - name: srv-api
  - name: ticket-intelligence-sys-html5-repo-host
    parameters:
      config:
        HTML5Runtime_enabled: true
      content-target: true
  build-parameters:
    build-result: app/
    requires:
    - artifacts:
      - mysupportticketdashboard.zip
      name: mysupportticketdashboard
      target-path: app/
- name: mysupportticketdashboard
  type: html5
  path: app/ticketdashboard
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ticket-intelligence-sys-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
        redirect-uris:
        - https://*~{app-api/app-uri}/**
      tenant-mode: dedicated
      xsappname: ticket-intelligence-sys-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
  requires:
  - name: app-api
- name: ticket-intelligence-sys-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: ticket-intelligence-sys-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
- name: ticket-intelligence-sys-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: ticket-intelligence-sys-html5-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: ticket-intelligence-sys-logging
  type: org.cloudfoundry.managed-service
  parameters:
    service: application-logs
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production